export const getCurrentRate = (
  fromCurrency: string,
  toCurrency: string
): number => {
  return 85;
};

export const convertCurrency = (
  amount: number,
  fromCurrency: string,
  toCurrency: string
): number => {
  return 100;
};
